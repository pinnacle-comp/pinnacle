name: Run Clippy

on:
  push:
    branches:
      - "main"
  pull_request:
    branches:
      - "main"

jobs:
  clippy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Clippy
        run: cargo clippy
